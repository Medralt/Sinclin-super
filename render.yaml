services:
  - type: web
    name: sinclin-runtime
    runtime: node
    plan: starter

    buildCommand: |
      cd staging/api
      npm install

    startCommand: |
      cd staging/api
      node server.js

    healthCheckPath: /health

    envVars:
      - key: NODE_ENV
        value: production
