services:
  - type: web
    name: sinclin-runtime
    runtime: node
    plan: starter

    buildCommand: |
      cd api
      npm install

    startCommand: |
      cd api
      node server.js

    healthCheckPath: /health

    envVars:
      - key: NODE_ENV
        value: production
